
  \begin{itemize}
  \item
    ket vectors are elements of the Hilbert space:
    $|\psi\rangle\in\mathcal{H}$
  \item
    bra vectors are elements of the adjoint/dual space:
    $\langle\psi|\in\mathcal{H}^\dagger$
  \item
    $\bra{\psi}\ket{\equiv\left(|\psi}\right)^\dagger$
  \item
    $\bra{\psi}\ket{\colon|\phi}\to c\in \mathbb{C}$
  \item
    $\bra{\psi}\ket{\colon|\psi}\to \bra{\psi}\ket{\psi} = ||\psi||^2$,
    the squared norm of the vector
  \end{itemize}
\end{itemize}

\begin{equation}
|\psi\rangle = a_+|z+\rangle + a_-|z-\rangle
\end{equation}

\begin{equation}
|\phi\rangle = b_+|z+\rangle + b_-|z-\rangle
\end{equation}

\begin{equation}
\langle\psi| = \langle z+|a_+^* + \langle z-|a_-^*
\end{equation}

$\bra{\psi}\ket{\psi} = |a_+|^2 + |a_-|^2$ because
